---
import { CASE_STUDIES } from '@/lib/content';
---

<section class="py-24 md:py-32 bg-[#FCF6F8] border-b border-[#F0DEE4] relative overflow-hidden">
  <!-- Subtle ambient radial raspberry light -->
  <div class="pointer-events-none absolute top-10 left-1/2 -translate-x-1/2 w-[900px] h-[450px] bg-gradient-to-b from-[#FB4566]/6 via-[#FB4566]/3 to-transparent rounded-full blur-[100px]" aria-hidden="true"></div>

  <div class="container-wide relative z-10">
    
    <!-- Section Header -->
    <div class="text-center max-w-3xl mx-auto mb-16">
      <div class="chip-synthesia mb-4">
        <span>Verified Outcomes</span>
      </div>
      <h2 class="headline-lg text-[#0E090D] [text-wrap:balance]">
        Scale, convert, and operate with discipline.
      </h2>
      <p class="mt-4 body-lg text-[#3A2A33] [text-wrap:balance]">
        Real outcomes delivered for owner-led businesses who traded fragmented agency chaos for an accountable digital department.
      </p>
    </div>

    <!-- 4 Customer Cards -->
    <div class="grid gap-6 sm:grid-cols-2 lg:grid-cols-4">
      {CASE_STUDIES.map((c) => (
        <div class="card-synthesia p-7 flex flex-col justify-between hover:scale-[1.01] transition-transform">
          <div>
            <div class="flex items-center justify-between gap-2 mb-4">
              <span class="text-[11px] font-mono font-bold uppercase tracking-wider px-2.5 py-1 rounded-full bg-[#FFF2F5] text-[#BE123C] border border-[#F0DEE4]">
                {c.sector}
              </span>
              <div class="flex text-amber-400 text-xs">
                ★★★★★
              </div>
            </div>

            <!-- Light Blush Tinted Metric Box -->
            <div class="my-4 p-4 rounded-2xl bg-[#FFF2F5] border border-[#F0DEE4]">
              <div class="text-3xl font-extrabold text-[#BE123C] font-mono tracking-tight">
                {c.stat}
              </div>
              <div class="text-xs font-mono font-bold mt-1 text-[#BE123C]">
                {c.statLabel}
              </div>
            </div>

            <p class="text-sm text-[#3A2A33] leading-relaxed italic">
              "{c.quote}"
            </p>
          </div>

          <div class="mt-6 pt-4 border-t border-[#F0DEE4]">
            <div class="text-sm font-bold text-[#0E090D] flex items-center justify-between">
              <span>{c.author}</span>
              <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-[#FB4566]">
                <circle cx="12" cy="12" r="10"/><path d="m9 12 2 2 4-4"/>
              </svg>
            </div>
            <div class="text-xs text-[#7A636E] mt-0.5">
              {c.role}
            </div>
            <div class="text-[11px] font-mono text-[#7A636E] mt-1">
              {c.client} • {c.location}
            </div>
          </div>
        </div>
      ))}
    </div>

    <!-- Case Studies Link -->
    <div class="mt-14 text-center">
      <a href="/case-studies" class="button-secondary">
        <span>View Detailed Case Studies</span>
        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M5 12h14"/><path d="m12 5 7 7-7 7"/>
        </svg>
      </a>
    </div>

  </div>
</section>
